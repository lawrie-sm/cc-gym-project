<h2>Edit <%= @member.full_name %></h2>

<form action="/members" method="post">
  <label for="first-name">First name</label>
  <input id="first-name" type="text" name="first_name" value="<%= @member.first_name %>">

  <label for="last-name">Last name</label>
  <input id="last-name "type="text" name="last_name" value="<%= @member.last_name %>">

  <label for="membership">Membership</label>
  <select id="membership" name="membership">
    <option <%= @member.membership == "basic" ? "selected" : "" %> value="basic">Basic</option>
    <option <%= @member.membership == "premium" ? "selected" : "" %> value="basic">Premium</option>
  </select>

  <button type="submit">Update Member</button>
</form>
