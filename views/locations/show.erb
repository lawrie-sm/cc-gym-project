<h2><%= @location.print_name %></h2>
<p><strong>Capacity: <%= @location.capacity %></strong></p>
<a href="/locations/<%= @location.id %>/edit">Edit</a>

<hr>

<h4>Event List</h4>
<ul class="item-list">
  <% @location.events.each do |e| %>
    <li>
      <a href="/events/<%= e.id %>"><%= e.name %></a> - <%= e.print_date %>
    </li>
  <% end %>
</ul>
