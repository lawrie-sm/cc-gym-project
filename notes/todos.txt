- Add some code for dealing with peak / off peak times
- Add recurring events
- Add locations, prevent two events at the same location at the same time
- Prevent customers from being added to multiple events at the same time
- Add a calender view with upcoming events
- Add memberships into the DB, with pics
- add pics/logos for events
- Add pics to users
- Use CSS text-transform to capitalise words
- Dynamically update the navbar based on location
  (i.e no "home" while on home page)
